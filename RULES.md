# Terminal Rules

IMPORTANT:
- Never use CMD syntax inside PowerShell
- Always use native PowerShell commands
- Use Get-ChildItem instead of dir /s /b
- Use Remove-Item instead of del /q
- Assume terminal shell is PowerShell on Windows
- Before executing terminal commands, verify PowerShell syntax